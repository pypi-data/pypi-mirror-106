# auto-generated by pywriter.py, do not edit or commit

from openpower.decoder.isa.caller import inject, instruction_info
from openpower.decoder.helpers import (EXTS, EXTS64, EXTZ64, ROTL64, ROTL32,
                                 MASK, MASK32,
                                 ne, eq, gt, ge, lt, le, ltu, gtu, length,
                                 trunc_divs, trunc_rems, MULS, DIVS, MODS,
                                 EXTS128, undefined,
                                 DOUBLE, SINGLE,
                                 FPADD32, FPSUB32, FPMUL32, FPDIV32,
                                 FPADD64, FPSUB64, FPMUL64, FPDIV64,
                                )
from openpower.decoder.selectable_int import SelectableInt
from openpower.decoder.selectable_int import selectconcat as concat
from openpower.decoder.orderedset import OrderedSet


from openpower.decoder.helpers import (
                                 DOUBLE, SINGLE,
                                 FPADD32, FPSUB32, FPMUL32, FPDIV32,
                                 FPADD64, FPSUB64, FPMUL64, FPDIV64,
                                )

from openpower.decoder.isafunctions.fpfromint import INT2FP
class fpcvt:
    @inject()
    def op_fcfids(self, FRB):
        FRT = INT2FP(FRB, 'sint2single')
        return (FRT,)

    @inject()
    def op_fcfids_(self, FRB):
        FRT = INT2FP(FRB, 'sint2single')
        return (FRT,)

    fpcvt_instrs = {}
    fpcvt_instrs['fcfids'] = instruction_info(func=op_fcfids,
                read_regs=OrderedSet(['FRB']),
                uninit_regs=OrderedSet(), write_regs=OrderedSet(['FRT']),
                special_regs=OrderedSet(), op_fields=OrderedSet(),
                form='X',
                asmregs=[['FRT', 'FRB'], '(Rc=0)'])
    fpcvt_instrs['fcfids.'] = instruction_info(func=op_fcfids_,
                read_regs=OrderedSet(['FRB']),
                uninit_regs=OrderedSet(), write_regs=OrderedSet(['FRT']),
                special_regs=OrderedSet(), op_fields=OrderedSet(),
                form='X',
                asmregs=[['FRT', 'FRB'], '(Rc=1)'])
