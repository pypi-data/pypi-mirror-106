id: "{deployment_id}"
containers:
  "{container_name}":
    image: "{image}"
    python_main: "{python_main}"
    command_line_arguments:
      worker_config:
        worker_id: "{deployment_id}"
        payload: "{payload}"
